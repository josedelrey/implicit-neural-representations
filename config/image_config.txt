# Task type
task = image

# Path to input image
image_path = images/test.jpg

export_path = outputs/test_reconstructed.png

# Whether the image is RGB (True/False)
is_rgb = True

# Side length to crop/resize the image to
sidelength = 256

# Total number of training steps
total_steps = 100

# How often (in steps) to print training logs
log_interval = 10

# Chunk size used during full-image evaluation/rendering
chunk_size = 4096

# Which model to use
model_type = waveletnetnormalized
